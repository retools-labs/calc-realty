// ============================================================
// X-45 후킹 — 한 화면에 하나만 보이게 하는 상태와 목적지. (2026-09-06)
//
// X-30 개정 정본(지시 039 판정 4):
//   「계산이 끝난 자리에는 단추를 놓지 않는다.
//     계산기가 못 하는 것을 하려는 순간에는 단추를 놓는다.」
//   「한 화면에 후킹은 하나만 보인다.」
//
// 세 자리(정산 줄 · 챙겨가기 줄 · 파트너 배너)가 같은 곳으로 가므로 겹치면 안 된다.
// 늦은 신호가 이긴다: 챙겨가기 > 정산 > 배너.
// ============================================================
import { POLICY_BASE_URL } from "./retoolsInfo";

export type Hook = null | "settle" | "carry";
export type ShowHook = (next: Exclude<Hook, null>) => void;

// [2026-09-04 R-18] 리얼티북의 옛 도메인을 직접 가리키고 있었다. 푸터의 약관 링크와
// 같은 문제인데 지시에는 이 한 줄이 빠져 있어 함께 잡는다. 옛 주소도 아직 열리기는
// 하지만, 정본 대장에서 사라져야 할 이름이 대외 화면에 남아 있으면 언젠가 그 주소가
// 서류로 옮겨 적힌다. 주소는 lib/retoolsInfo.ts 한 곳에서만 정한다.
//
// [X-45] 후킹 두 줄도 배너와 같은 곳으로 간다. 주소를 새로 만들지 않는다.
export const PARTNER_URL = `${POLICY_BASE_URL}/partner`;
