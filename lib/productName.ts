// ============================================================
// 장부 제품의 이름을 이 저장소에서 읽는 자리. — R-20 (2026-09-05)
//
// 계산기 자신의 이름(복비 계산기)은 바뀌지 않았다. 다만 이 화면 곳곳이 장부 제품을
// 가리키고 있어서 그 이름이 함께 바뀐다. 「리얼티북」에서
// 「오늘하루 장부-부동산중개」로 간다.
//
// 정본은 realtybook 저장소의 src/lib/productName.ts 다. 저장소가 달라 코드로 묶이지
// 않으므로 값을 옮겨 적는다. 사업자정보(retoolsInfo.ts)와 같은 구조이고, 같은 이유로
// scripts/check-retools-info.ts 가 어긋남을 빌드 전에 잡는다. R-18 에서 배운 것이다.
// 주석으로 「두 곳 다 고쳐야 한다」고 적는 방법은 이미 한 번 실패했다.
// ============================================================

/** 정식 명칭. */
export const PRODUCT_NAME = "오늘하루 장부-부동산중개";

/** 좁은 자리에서 쓰는 줄임말. */
export const PRODUCT_NAME_SHORT = "오늘하루 장부";

/** 옛 이름. 「(구 리얼티북)」으로만 병기한다(총괄 지시 032 2절). */
export const PRODUCT_NAME_LEGACY = "리얼티북";

/**
 * 이 계산기 자신의 이름. 정본(C:\dev\정본.md 2절)의 제품 표에 적힌 값이다.
 *
 * [2026-09-05 지시 036 부속 1] 이름이 네 갈래로 적혀 있던 것을 총괄이 정리했다.
 * 설치된 앱의 이름을 말하는 자리(web manifest · 홈 화면 · 설치 안내)는 전부 이 값이다.
 * 스토어 등재명 「부동산 계산기 - 오늘하루」와 안드로이드 번들의
 * 「부동산 계산기 (by 오늘하루)」는 각 스토어의 표기 규칙을 따른 변형이고,
 * 그 둘은 이 저장소가 아니라 콘솔과 C:\android-build 에 있다.
 *
 * 화면 안의 「복비 계산」 같은 이름은 제품명이 아니라 기능 이름이라 여기서 정하지 않는다.
 * 기능 이름은 app/page.tsx 의 TABS 가 정본이다(지시 036 0절).
 */
export const CALC_PRODUCT_NAME = "부동산 계산기";
