# calc-realty — Codex 작업 규칙

1. 작업 루트의 `AGENTS.md`(공통 규칙)를 먼저 읽는다.
2. 이 저장소의 규칙은 같은 폴더의 `CLAUDE.md` 에 있다. 이름만 Claude 용이고 **이 저장소의 규칙 전부**이니 끝까지 읽고 따른다.
3. 두 파일이 다르면 더 엄격한 쪽을 따르고, 다른 점을 결과에 적는다.
4. main 에 직접 push·병합·배포하지 않는다. `RTLS-DEV/<task_id>` 브랜치와 PR 로만 낸다.

